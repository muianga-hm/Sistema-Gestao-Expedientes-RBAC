spring.application.name=sistema-expedientes

# MySQL

spring.datasource.url=jdbc:mysql://localhost:3306/sistema_expedientes?useSSL=false&serverTimezone=Africa/Maputo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Msond@2026

# JPA / Hibernate

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf

spring.thymeleaf.cache=false

# Docker Compose desativado

spring.docker.compose.enabled=false

# Sessão utilizando cookies

server.servlet.session.tracking-modes=cookie
